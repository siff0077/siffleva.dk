"use strict"

window.addEventListener('load', fade_in);

function fade_in(){ 
    /* document.querySelector("#splash_p").classList.add("fade_in"); */

    /* gsap.registerPlugin(ScrollTrigger);
  gsap.to('progress', {
    value: 100,
    ease: 'none',
    scrollTrigger: { scrub: 0.3 }
  });



  gsap.registerPlugin(ScrollTrigger);
        gsap.to("section",{x: "10vw", scrollTrigger:{trigger:"#box1",scrub:true}}); */
        

}